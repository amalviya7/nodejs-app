# Environment variables template
# Copy this file to .env and fill in the actual values for local development

# Port for the application
PORT=3000

# AWS Region for Secrets Manager
AWS_REGION=us-east-1

# Name of the secret in AWS Secrets Manager containing MongoDB URI
MONGODB_SECRET_NAME=mongodb_uri

# For local development, you may need to set MONGODB_URI directly if not using Secrets Manager
# MONGODB_URI=mongodb://localhost:27017/nodeapp

# MongoDB root password for docker-compose (local development)
# MONGO_ROOT_PASSWORD=your_secure_password_here
